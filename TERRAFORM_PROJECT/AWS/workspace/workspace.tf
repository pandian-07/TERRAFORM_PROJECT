# terraform workspace new teraworkspace   - it will create the workspace with the name tera workspace
# terraform workspace show                - it will show the current workspace name
# terraform workspace select teraworkspace- it will switch to the worksapce which we giving
# terraform workspace list                - it will list the all the workspace
# terraform workspace delete teraworkspace- it will delete the workspace as per the giving workspace name 
